<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:primitives="clr-namespace:CommunityToolkit.Maui.Core.Primitives;assembly=CommunityToolkit.Maui.Core"
             xmlns:viewmodels="clr-namespace:TVPlayerMAUI.ViewModels"
             xmlns:models="clr-namespace:TVPlayerMAUI.Models"
             xmlns:views="clr-namespace:TVPlayerMAUI.Views"
             x:Class="TVPlayerMAUI.Views.MainPage"
             x:DataType="viewmodels:MainPageViewModel"
             Title="TVPlayer Desktop"
             BackgroundColor="Black"
             NavigationPage.HasNavigationBar="False"
             Shell.NavBarIsVisible="False">

    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
            <toolkit:IsEqualConverter x:Key="IsEqualConverter" />
            <views:TimeSpanToTimeStringConverter x:Key="TimeSpanConverter" />
            <Style x:Key="HideOnFullScreenStyle" TargetType="VisualElement">
                <Setter Property="IsVisible" Value="True" />
                <Style.Triggers>
                    <DataTrigger TargetType="VisualElement" Binding="{Binding IsVideoFullScreen}" Value="True">
                        <Setter Property="IsVisible" Value="False" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid ColumnDefinitions="Auto, *">
        <Grid x:Name="SideMenuGrid" Grid.Column="0" WidthRequest="300" BackgroundColor="#1E1E1E" Style="{StaticResource HideOnFullScreenStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- Área de Input -->
                <RowDefinition Height="*" />
                <!-- Listas (Canais/Grupos) -->
                <RowDefinition Height="Auto" />
                <!-- Rodapé (Configurações) -->
            </Grid.RowDefinitions>

            <VerticalStackLayout Grid.Row="0" Spacing="10" Padding="10" BackgroundColor="#2D2D2D">
                <Label Text="TVPlayer Desktop" FontAttributes="Bold" TextColor="White" HorizontalOptions="Center"/>
                <Entry x:Name="M3uUrlEntry" Placeholder="Cole a URL .m3u" Text="{Binding M3uUrl}" TextColor="White" PlaceholderColor="Gray"/>
                <Button Text="Carregar Lista" Command="{Binding LoadChannelsCommand}" IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}" BackgroundColor="#512BD4" TextColor="White"/>
            </VerticalStackLayout>

            <Grid Grid.Row="1" Padding="5">
                <Grid RowDefinitions="Auto, Auto, Auto, *">
                    <Button Grid.Row="0" Text="&lt; Voltar aos Grupos" Command="{Binding GoBackToGroupsCommand}" IsVisible="{Binding IsShowingChannelsInGroup}" Margin="0,5" BackgroundColor="Transparent" TextColor="#512BD4" BorderColor="#512BD4" BorderWidth="1"/>

                    <Label Grid.Row="1" Text="{Binding HeaderTitle}" FontSize="14" FontAttributes="Bold" TextColor="White" Padding="5" />
                    <SearchBar x:Name="ChannelSearchBar" Grid.Row="2" Placeholder="Buscar..." Text="{Binding SearchText}" BackgroundColor="#333" TextColor="White" PlaceholderColor="Gray" Margin="0,5"/>

                    <CollectionView Grid.Row="3" x:Name="GroupsCollectionView" ItemsSource="{Binding GroupSummaries}" IsVisible="{Binding IsShowingGroups}" SelectedItem="{Binding SelectedGroup, Mode=TwoWay}" SelectionMode="Single">
                        <CollectionView.Behaviors>
                            <toolkit:EventToCommandBehavior EventName="SelectionChanged" Command="{Binding SelectGroupCommand}" CommandParameter="{Binding Source={x:Reference GroupsCollectionView}, Path=SelectedItem}" />
                        </CollectionView.Behaviors>
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:ChannelGroupSummary">
                                <Border Padding="10" Margin="2" Stroke="Gray" StrokeThickness="0" BackgroundColor="#2D2D2D" StrokeShape="RoundRectangle 5">
                                    <HorizontalStackLayout Spacing="5">
                                        <Label Text="{Binding Name}" FontAttributes="Bold" TextColor="White" VerticalOptions="Center"/>
                                        <Label Text="{Binding ChannelCount, StringFormat='({0})'}" TextColor="Gray" VerticalOptions="Center"/>
                                    </HorizontalStackLayout>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <CollectionView Grid.Row="3" x:Name="ChannelsCollectionView" ItemsSource="{Binding ChannelsInView}" IsVisible="{Binding IsShowingChannels}" SelectedItem="{Binding SelectedChannel, Mode=TwoWay}" SelectionMode="Single">
                        <CollectionView.Behaviors>
                            <toolkit:EventToCommandBehavior EventName="SelectionChanged" Command="{Binding SelectChannelCommand}" CommandParameter="{Binding Source={x:Reference ChannelsCollectionView}, Path=SelectedItem}" />
                        </CollectionView.Behaviors>
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:Channel">
                                <Border Padding="10" Margin="2" StrokeThickness="0" BackgroundColor="#333" StrokeShape="RoundRectangle 5">
                                    <Label Text="{Binding Name}" TextColor="White" VerticalOptions="Center" LineBreakMode="TailTruncation"/>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Grid>
            </Grid>

            <Button Grid.Row="2" 
                    Text="Configurações / Atualizações" 
                    ImageSource="settings_gear.svg"
                    Command="{Binding ShowSettingsCommand}" 
                    BackgroundColor="Transparent" 
                    TextColor="Gray" 
                    Margin="10" 
                    BorderColor="Gray" 
                    BorderWidth="1"/>
        </Grid>

        <Grid x:Name="VideoContainer" Grid.Column="1" BackgroundColor="Black">
            <Grid.GestureRecognizers>
                <PointerGestureRecognizer PointerMoved="FullScreen_PointerMoved" />
            </Grid.GestureRecognizers>

            <toolkit:MediaElement x:Name="mediaPlayer" 
                                  Source="{Binding VideoSource}" 
                                  ShouldAutoPlay="True" 
                                  ShouldShowPlaybackControls="False" 
                                  HorizontalOptions="Fill" 
                                  VerticalOptions="Fill" 
                                  PositionChanged="mediaPlayer_PositionChanged" 
                                  StateChanged="MediaPlayer_StateChanged"/>

            <ActivityIndicator Color="White" HorizontalOptions="Center" VerticalOptions="Center"
                               IsRunning="{Binding Source={x:Reference mediaPlayer}, Path=CurrentState, Converter={StaticResource IsEqualConverter}, ConverterParameter=Buffering}" />

            <Button x:Name="BtnToggleMenu"
                    Text="☰"
                    Style="{StaticResource HideOnFullScreenStyle}"
                    FontSize="24"
                    TextColor="White"
                    BackgroundColor="#80000000"
                    HorizontalOptions="Start"
                    VerticalOptions="Start"
                    Margin="10"
                    Clicked="BtnToggleMenu_Clicked"
                    ZIndex="10"/>

            <Grid x:Name="VideoControlsOverlay" Opacity="0" BackgroundColor="Transparent" InputTransparent="False">
                <Border VerticalOptions="End" Padding="10" Margin="20" StrokeThickness="0" BackgroundColor="#CC000000" StrokeShape="RoundRectangle 10">
                    <VerticalStackLayout Spacing="10">
                        <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="10">
                            <Label Grid.Column="0" Text="{Binding Position, Converter={StaticResource TimeSpanConverter}}" TextColor="White" VerticalOptions="Center"/>
                            <Slider Grid.Column="1" x:Name="timelineSlider" Maximum="{Binding Source={x:Reference mediaPlayer}, Path=Duration.TotalSeconds}" Value="{Binding Position.TotalSeconds}" DragCompleted="timelineSlider_DragCompleted" ThumbColor="#512BD4" MinimumTrackColor="#512BD4"/>
                            <Label Grid.Column="2" Text="{Binding Source={x:Reference mediaPlayer}, Path=Duration, Converter={StaticResource TimeSpanConverter}}" TextColor="White" VerticalOptions="Center"/>
                        </Grid>

                        <HorizontalStackLayout Spacing="15" HorizontalOptions="Center" VerticalOptions="Center">
                            <Button x:Name="PlayPauseButton" Text="Pause" Clicked="PlayPauseButton_Clicked" WidthRequest="80" BackgroundColor="Transparent" TextColor="White" BorderColor="White" BorderWidth="1"/>
                            <Button x:Name="MuteButton" Text="Mudo" Clicked="MuteButton_Clicked" WidthRequest="80" BackgroundColor="Transparent" TextColor="White" BorderColor="White" BorderWidth="1"/>

                            <Label Text="Volume:" VerticalOptions="Center" TextColor="White"/>
                            <Slider Maximum="1" Minimum="0" WidthRequest="150" Value="{Binding Source={x:Reference mediaPlayer}, Path=Volume, Mode=TwoWay}" ThumbColor="White" MinimumTrackColor="White" />

                            <Button x:Name="FullScreenButton" Command="{Binding ToggleVideoFullScreenCommand}" WidthRequest="150" BackgroundColor="#512BD4" TextColor="White">
                                <Button.Triggers>
                                    <DataTrigger TargetType="Button" Binding="{Binding IsVideoFullScreen}" Value="True">
                                        <Setter Property="Text" Value="Sair Tela Cheia" />
                                    </DataTrigger>
                                    <DataTrigger TargetType="Button" Binding="{Binding IsVideoFullScreen}" Value="False">
                                        <Setter Property="Text" Value="Tela Cheia" />
                                    </DataTrigger>
                                </Button.Triggers>
                            </Button>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Border>
            </Grid>
        </Grid>

        <Grid IsVisible="{Binding IsLoading}" Grid.ColumnSpan="2" BackgroundColor="#CC000000">
            <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="10">
                <ActivityIndicator IsRunning="{Binding IsLoading}" Color="White" />
                <Label Text="{Binding LoadingStatus}" TextColor="White" HorizontalOptions="Center" />
            </VerticalStackLayout>
        </Grid>
    </Grid>
</ContentPage>